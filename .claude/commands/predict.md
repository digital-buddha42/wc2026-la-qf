Run the WC 2026 LA Quarterfinal predictor and display results.

Usage: /predict [--no-fetch] [--trials N]

Run the following bash command and show the output to the user:

```bash
cd /home/user/wc2026-la-qf && python main.py $ARGUMENTS
```
